# MBTI 공부법 연구소

## 프로젝트
기질 그룹별(NT·NF·SJ·SP) 공부법을 소개하고,
10문항 자가진단으로 내 유형을 찾아주는 html 멀티페이지 콘텐츠 사이트.

## 디자인
디자인 규칙은 `BMW_m_DESIGN.md`를 따른다. (기존 크림+보라 규칙은 폐기)

- 배경: 순수 블랙 `--canvas` #000. 라이트 모드 없음
- 포인트 컬러: M 트라이컬러 (#0066b1 → #1c69d4 → #e22718). 4px 스트라이프 디바이더와
  그룹 식별 액센트로만 쓴다. 버튼 배경으로 쓰지 않는다
- 기질 그룹 액센트는 M 트라이컬러와 별도 토큰이다. 네 그룹이 한 화면에서
  구분돼야 하므로 NT=#22c8e8, NF=m-red, SJ=#3d82e8, SP=#f4b400.
  값을 바꿀 때는 ① 그룹 간 최소 색차 dE 40 이상 ② 검정 배경 대비 4.5:1 이상
  두 기준을 반드시 다시 확인할 것 (트라이컬러 원값을 그대로 쓰면 NT/SJ가 dE 20으로 겹친다)
- 폰트: Pretendard (CDN, @v1.3.9 버전 고정) — BMW Type Next Latin의 대체 폰트.
  디스플레이 700 / 본문 300 두 굵기만 쓴다. 본문을 400 이상으로 올리지 않는다
- 모서리: 0px가 전역 기본. 원형 아이콘 버튼만 예외
- 그림자 금지. 깊이는 canvas ↔ surface-card 대비로만 만든다
- 사진 대신 인라인 SVG로 기하학적 히어로를 그린다 (외부 이미지 요청 0건)
- 대문자 라벨은 트래킹 1.5px 고정. 한글 헤드라인은 700 + 음수 트래킹으로 대체
- 밴드 배경을 연속으로 반복하지 않는다 (canvas → card → canvas → soft 순으로 교차)
- 모바일 반응형 필수 — 모바일 우선으로 작성. 확장은 min-width로만

## 규칙
- 백엔드는 Vercel 서버리스 함수(api/)로만 둔다. 상시 구동 서버는 금지.
  현재 함수는 api/count.js 하나뿐이다 — 진단 결과 집계.
- 비밀 키는 Vercel 환경변수에만 둔다. 저장소에 커밋 금지.
  프런트 코드에는 공개 값만 넣는다 (GA4 측정 ID는 공개 값이라 무방).
- 외부 요청 허용 범위: Pretendard 폰트 CDN, GA4(G-DS02G41KEY), 자체 /api.
- 진단 답변 내용은 전송하지 않는다. GA4 이벤트는 두 개뿐이다 —
  test_start(첫 응답)와 test_complete(판정된 그룹).
  이 범위를 넘기면 test.html·index.html의 개인정보 문구도 함께 고쳐야 한다.
- 파일이 300줄을 넘으면 코드를 더 쓰기 전에 분리 방안을 먼저 제안할 것.
- 페이지 = 파일 하나. index / nt / nf / sj / sp / test (총 6개)
- 페이지 이동은 반드시 a 태그. JS 라우팅 금지.
- 채점은 브라우저 JS로만 처리.
- 전체 응답자 분포는 /api/count가 실시간으로 집계한다. 손으로 갱신하지 않는다.
  표본이 MIN_SAMPLE(100) 미만이거나 집계가 실패하면 화면에 띄우지 않는다.

## 구조 규칙
- h1은 페이지당 1개
- header · nav · main · footer 시맨틱 태그 사용
- 모든 img에 alt 필수. 인라인 SVG는 role="img" + aria-label로 대신한다
- CSS 진입점은 style.css 한 파일. 300줄 규칙 때문에 실제 규칙은 아래 4개로 분리했고
  style.css는 @import만 갖는다:
  tokens.css(변수) / base.css(리셋·타이포·레이아웃) / chrome.css(헤더·히어로·푸터) /
  components.css(버튼·카드·폼·결과)
- 색은 전부 tokens.css의 :root에서만 정의한다. 다른 CSS 파일에 hex 인라인 금지.
  히어로 SVG의 장식용 스트로크만 예외로 리터럴 값을 허용한다

## 작업 방식
- 큰 변경 전에는 플랜을 먼저 보여줄 것
- 한 번에 한 페이지씩. 완료 후 요약 보고
